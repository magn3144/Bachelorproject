Extract the text and corresponding XPaths of all the `<a>` tags with class "brSCsc"
Extract the text and corresponding XPaths of all the `<span>` tags with class "iDvCIf xfudi"
Extract the text and corresponding XPaths of all the `<span>` tags with class "VfPpkd-StrnGf-rymPhb-b9t22c"
Extract the text and corresponding XPaths of all the `<div>` tags with class "EY8ABd-OWXEXe-TAWMXe" and id "tt-i23"
Extract the text and corresponding XPaths of all the `<p>` tags with class "jBYiqc"
Extract the text and corresponding XPaths of all the `<h1>` tags with class "Da1Shd"
Extract the text and corresponding XPaths of all the `<h4>` tags with class "JtKRv iTin5e"
Extract the text and corresponding XPaths of all the `<a>` tags with class "brSCsc"
Extract the text and corresponding XPaths of all the `<span>` tags with class "VfPpkd-StrnGf-rymPhb-b9t22c"
Extract the text and corresponding XPaths of all the `<div>` tags with class "vr1PYe"
